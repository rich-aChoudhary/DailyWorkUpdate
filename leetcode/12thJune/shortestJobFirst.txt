class Solution:
    def solve(self, bt):
        bt.sort()
        t,wt=0,0
        
        for i in range(len(bt)):
            wt+=t
            t+=bt[i]
            
        return wt//len(bt)
        