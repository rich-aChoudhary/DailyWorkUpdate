class Solution:
    def maxValidPairSum(self, nums: list[int], k: int) -> int:
        
        res=float('-inf')
        maxSum=nums[0]
        for j in range(k,len(nums)):
            maxSum=max(maxSum,nums[j-k])
            res=max(res,maxSum+nums[j])
                
        return res