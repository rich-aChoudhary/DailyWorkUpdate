class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        n=len(nums)
        
        count=0
        maxC=0
        for i in range(n):
            if nums[i]==1:
                count+=1
                maxC=max(count,maxC)
            else:
                count=0

        return maxC
