class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        curSum=0
        maxSum=float('-inf')

        for i in range(len(nums)):
            curSum+=nums[i]
            maxSum=max(curSum,maxSum)
            if curSum<0:
                curSum=0

        return maxSum