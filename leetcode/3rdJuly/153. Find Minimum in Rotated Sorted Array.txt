class Solution:
    def findMin(self, nums: List[int]) -> int:
        st=0
        last=len(nums)-1
        
        while st<last:
            mid=(st+last)//2

            if nums[mid]>nums[last]:
                st=mid+1
            else:
                last=mid

        return nums[st]
