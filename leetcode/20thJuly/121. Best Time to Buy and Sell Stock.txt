class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        maxPr=float('-inf')
        minPrice=prices[0]

        for p in prices:
            minPrice=min(p,minPrice)
            maxPr=max(maxPr,p-minPrice)

        return maxPr